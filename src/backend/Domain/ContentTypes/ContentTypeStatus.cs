namespace Domain.ContentTypes;

/// <summary>
/// Enumeration of possible states for a content type definition.
/// </summary>
public enum ContentTypeStatus
{
    DRAFT,
    ARCHIVE,
    IN_REVIEW,
    SCHEDULED,
    CURRENTLY_USED,
}
